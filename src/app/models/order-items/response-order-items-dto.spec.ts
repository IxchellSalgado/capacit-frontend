import { ResponseOrderItemsDTO } from './response-order-items-dto';

describe('ResponseOrderItemsDTO', () => {
  it('should create an instance', () => {
    expect(new ResponseOrderItemsDTO()).toBeTruthy();
  });
});
