import { RequestOrderItemsDTO } from './request-order-items-dto';

describe('RequestOrderItemsDTO', () => {
  it('should create an instance', () => {
    expect(new RequestOrderItemsDTO()).toBeTruthy();
  });
});
